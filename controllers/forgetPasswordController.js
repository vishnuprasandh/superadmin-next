// Placeholder for forget password controller logic
export const forgetPasswordController = ({ mobileNumber, otp }) => {
    console.log(`Requesting password reset for mobile number: ${mobileNumber} with OTP: ${otp}`);
    // You can add actual forget password logic here if needed
  };
  
  // Placeholder for OTP resend logic
  export const resendOtpController = ({ mobileNumber }) => {
    console.log(`Resending OTP to mobile number: ${mobileNumber}`);
    // You can add actual OTP resend logic here if needed
  };
  
// Placeholder for login controller logic
export const loginController = ({ mobileNumber, otp }) => {
    console.log(`Logging in with mobile number: ${mobileNumber} and OTP: ${otp}`);
    // You can add actual login logic here if needed
  };
  